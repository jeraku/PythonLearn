# One more and one less
# Write a Python program that takes a number as input and prints the number that comes immediately before it and immediately
#  after it

number = int(input("Enter your input value: "))
before =number-1
after  = number+1
print(f"before value is {before} and after value is {after}")