Multimodal > it serves for Image/ doc everything.
pure LLM is a pure text based converstion.

### Genreative AI
inclusive and diversirty > temperature and top p 
results will be shared with the probability next text will be created and shared

### Agentic AI
series of steps to achieve the goal
input -> reason -> act -> learn
example.
chatbot, virtual assisstant, self driving cars

connnecting to tools
DB/email db/CRM system

## Key components
# Sensors 
how agent is collecting infomration are from sensors.
collect data from different real time details 
1) scraping 
2) accessing 
3) interpreting images from camera
4) capturing audio
5) analysing data from IOT devices.

# Model
retreive data from different sources
# Decision making Logic
from collected source of data, make a decision based on the parameters.

# Actuator
from where the action to be performed. Allow agent to interact with physical applications.
it calls an API to order something.
physical actuator
Virtual actutor as well ex: send spam email to spam folder.

EX: 
1) buy or sell order in stock market. 
2) self driving car.

# Feedback Mechanism
netflix recommendation 
amazon product display


## Characteristics for Agent AI.
- profile/Persona (you are an advisor//you are an investment banker)
- Memory (Long term memory and short term memory) -> customer service chatbot -> share domain info to agent 
- Reasoning (use available information and solve complex problems)
- Actions (agent changes its environment - post info in multiple social media content) > from actuator
- Learning capabilities (learn from feedback and again response with new info )

## Types of AI agent

# Simple reflex agent
do what we say
Ex: Thermosatat in our home
# Model based reflex agent.
it needs a internal memory, create a image within itself and works
Ex: a robot vaccum cleaner - it covers sofa, corners, cleaned area.
# Goal based agent
Ex: Google maps, Waze 
# Utility based agents
two option 7% return and 2% return - but this agent also consider other possibilities. agent commission/ risk of stock etc..,
Ex: Investment advisor agents
# Learning Agent
work based on the feedback
Ex: self learning game agent, recommendatation from streaming apps.
Human in the loop.  HITL  Learning form humans.
Learning from external systems

Two way of learning
1) Learning from human
2) learning from external system.

Gen AI vs AI Agent
Gen AI > write an email
AI agents > reasearch, Write, and send the email 

LLM VS AI workflow  Vs AI agent
LLM > generate text only.
AI workflow > specific order of workflows> chatgpt + outlook email +  post in teams. > do what action has been defined.
AI agent > take action and decision > chatgpt + outlook email (research + Tools) +  post in teams.



