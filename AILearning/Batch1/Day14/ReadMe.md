### MCP
Agentic AI
builing AI Agents > Goal based agents
-> Video uploading
-> document from video.
-> timestamp 
-> create and publish blog for the video.
-> Update in telegram

LLM 
MCP -> Model context protocol -> Anthropic
MCP gives schema -> 
LLM takes input and call the tool.
{}
{method: "list Tools []"} 

public MCPs.
set of rules/protocol, to consume the same methods across the AI.

travily > AIAgents connect to internet.

SSE-> Server send events
event stream API. > conversation
standard input/output to speak with agents.

Anthropic/Fast API MCP agents > using these two we can create the MCP agents
FastMCP

Agent integrate -> Link LLM -> call MCP

