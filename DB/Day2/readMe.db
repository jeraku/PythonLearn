Roles, Users, Group

Roles
Ex: Developer, Admin
--------------
Users
Ex: Individual employees.
--------------
Group:
ex: specific group
--------------
in Postgres all are same


Role -> Identity(Developer) -> Cannot login, New DB creation not allowed -> group allowed
User-> Login as user -> whoc can login -> -U -> he can create DB -> group not allowed

Access
Login - User 
Another Super user  -> Admin
Create DB
Inherit-> (Developer roles can read and write)

peer authentication > Machine user authentication we can login into SQL.
passowrd authentication > MD5 authentication

psql -U jegan -d <DBNAME>

Basic select Queries.
command
select * form actor limit = 10;

EXPLAIN select * from actor;

select * from actor order by first_name ASC LIMIT 3;
select * from actor order by first_name ASC OFFSET 3 LIMIT 3;