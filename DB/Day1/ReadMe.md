DB: 

rigid structure

Schema : Blue print

In program: Variables are stored in the RAM memory. 
Disk: I/O operation requried - system will be slow.

Persistent storage:  stores in Disk / Survives Restart 
non persistent : redis (cache) store in RAM and use faster/ Survives in restart False.  (qualified for primary DB)
Redis in persistent mode is also allowed.

Graph (Neo4J)
>> map, statistical data, Vector DB, RAG arch.
>> form a graph between two nodes.

Obsidian DB.

Time series -> Influx DB (Log, stock analysing)>> Grafana can also be used in it.

Postgres
DB engine.  A pool of processes.

Client > Postmaster > Backend process pool > 



Postgres youtube call:
https://www.youtube.com/live/lo8VWgXUB08

DVD rental DB: 
https://courses.parottasalna.com/database-engineering/sample-databases/dvd-rental-database?authuser=4

Miro for DB schema details It contains Postgres architecture diagram as well
https://miro.com/app/board/uXjVLD2T5os=/?authuser=4

